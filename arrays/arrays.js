var dogs = ["Josie", "Doris", "Hammy", "Fay"];
var dogAges = [3,9,3,8];
var dogInfos = [true,true,false,true]

dogs.pop();

console.log(dogs);
//Removes the last element from an array and returns that element.

dogAges.slice();

console.log(dogAges)
//Extracts a section of the calling array and returns a new array.

dogInfos.push();

console.log(dogInfos)
//Adds one or more elements to the end of an array, and returns the new length of the array.

//The index of an array is the order of it's elements starting with 0 and moving up in lenght for example on the variable dogs "Hammy"'s index is 2

console.log(dogs [2])